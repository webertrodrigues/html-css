#include <stdio.h>

main()
{
    int x, y, z;
    Printf("Digite valor de x: ");
    scanf("%i", &x);
    Printf("Digite valor de y: ");
    scanf("%i", &y);
    z = x+y;
    printf("O valor da soma desses números é de %i", z);
}